# Student-Record-Management-System
Student Record Management System in C — a menu‑driven program to add, view, search, update, delete, and rank student records. Uses structures, arrays, functions, and file handling to store data permanently, compute totals/percentages, and sort by marks.
